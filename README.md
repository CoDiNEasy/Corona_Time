rename function.zip to function_v<*.*>.zip when a new stable version comes out

cd to root of IntelliJ project:
    'ask clone' to pull from remote
    'ask deploy' to push any model changes
    ./*.sh to push packaged code 